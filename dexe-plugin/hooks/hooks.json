{
  "description": "dexe-mcp hooks — first-run onboarding nudge (fires once).",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/sessionstart.mjs"
          }
        ]
      }
    ]
  }
}
